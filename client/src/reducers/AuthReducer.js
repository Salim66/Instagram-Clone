

// create auth reducer
const AuthReducer = ( state, { type, payload } ) => {

    switch (type) {
        case '':
            
            break;
    
        default:
            return state;
            break;
    }

}

// export auth reducer
export default AuthReducer;